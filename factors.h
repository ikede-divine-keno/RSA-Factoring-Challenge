#ifndef FACTORS_H
#define FACTORS_H
#define _GNU_SOURCE

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/* size_t getline(char **lineptr, size_t *n, FILE *stream); */
int _factorize(char *buff);

#endif /* _FACTORS_H_ */
